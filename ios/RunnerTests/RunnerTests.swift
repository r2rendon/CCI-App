import Foundation

// NOTE:
// This file exists because the Xcode project references `RunnerTests.swift`
// but it was missing from disk, which breaks `flutter run` for iOS.
//
// Some projects accidentally include this file in the main `Runner` target too,
// so keep it free of XCTest-only APIs to compile cleanly either way.

